apiVersion: apps/v1
kind: Deployment
metadata:
  name: telegram-service
  namespace: pdp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: telegram-service
  template:
    metadata:
      labels:
        app: telegram-service
    spec:
      containers:
        - name: telegram-service
          image: telegram-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8080

---
apiVersion: v1
kind: Service
metadata:
  name: telegram-service
  namespace: pdp
spec:
  type: NodePort
  selector:
    app: telegram-service
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30084
